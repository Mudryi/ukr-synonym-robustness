torch>=2.0
transformers>=4.50
sentence-transformers>=2.7
huggingface-hub>=0.20
pandas>=2.0
numpy>=1.24
tqdm>=4.65
PyYAML>=6.0
fasttext>=0.9.2
pymorphy2>=0.9.1
pymorphy2-dicts-uk>=2.4.1
accelerate>=0.30

# bitsandbytes is CUDA-only; skip on macOS (Apple Silicon uses MPS, no quant).
bitsandbytes>=0.43; platform_system != "Darwin"